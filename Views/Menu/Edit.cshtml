@model Blank.Models.Menu

@{
    Layout = "~/Views/Shared/AdminLO.cshtml";
}

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Edit Menu</h4>
                    </div>
                    <div class="card-body">
                        <form asp-action="Edit" enctype="multipart/form-data" method="post">
                            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                            <input type="hidden" asp-for="MenuId" />
                            <div class="mb-3">
                                <label asp-for="DishId" class="form-label">Dish ID</label>
                                <input asp-for="DishId" class="form-control" placeholder="Enter Dish ID" />
                                <span asp-validation-for="DishId" class="text-danger"></span>
                            </div>
                            <div class="mb-3">
                                <label asp-for="RestaurantId" class="form-label">Restaurant ID</label>
                                <input asp-for="RestaurantId" class="form-control" placeholder="Enter Restaurant ID" />
                                <span asp-validation-for="RestaurantId" class="text-danger"></span>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary">Save Changes</button>
                            </div>
                        </form>
                    </div>
                    <div class="card-footer text-center">
                        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

